# resource-tracker

Lightweight Linux resource & GPU tracker.

## Usage

```bash
resource-tracker [FLAGS] -- <command> [args...]
```

The tracker will spawn `<command>`, monitor it, and exit when it exits.
